#pragma once
#include "pch.h"
#include "afxdialogex.h"
#include <vector>
#include <string>

class CStock
{
public:
	CStock() {}
	//CStock(double openVal, double closeVal, double highVal,
	//	double lowVal, double baseVal, double currentVal,
	//	double volumeVal, double marketCapVal, double capitalVal,
	//	double faceValueVal, double salesVal, double operatingIncomeVal,
	//	double netIncomeVal, double outstandingShareVal, double ROEVal,
	//	double PERVal, double PBRVal, double EVVal, int numOfStockVal,
	//	int codeVal, CString stockNameVal, CString creditRatioVal) : 
	//	open(openVal) , close(closeVal), high(highVal), low(lowVal),
	//	base(baseVal), current(currentVal), volume(volumeVal),
	//	marketCap(marketCapVal), capital(capitalVal), faceValue(faceValueVal),
	//	sales(salesVal), operatingIncome(operatingIncomeVal),
	//	netIncome(netIncomeVal) , outstandingShare(outstandingShareVal),
	//	roe(ROEVal), pbr(PBRVal), per(PERVal), ev(EVVal), numOfStock(numOfStockVal),
	//	code(codeVal), stockName(stockNameVal), creditRatio(creditRatioVal) {}
	~CStock() {}
private:
	//int code;				// 종목코드
	//double open;			// 시가
	//double close;			// 종가
	//double high;			// 고가
	//double low;				// 저가
	//double base;			// 기준가
	//double current;			// 현재가
	//double volume;			// 거래량
	//double marketCap;		// 시가총액
	////double marketCapRatio;	// 시가총액비중
	////double foreignMaxRatio;	// 외인소진률
	//double capital;			// 자본금
	//double faceValue;		// 액면가
	//double sales;			// 매출액
	//double operatingIncome;	// 영업이익
	//double netIncome;		// 당기순이익
	//double outstandingShare;// 유통주식
	////double osShareRatio;	// 유통비율
	////double eps;				// EPS
	//double roe;				// ROE
	//double per;				// PER
	//double pbr;				// PBR
	//double ev;				// EV
	////double bps;				// BPS
	//CString stockName;		// 종목명
	//CString creditRatio;	// 신용비율
	//int numOfStock;			// 상장주식
public: 
	//double GetOpen() { return open; }
	//double GetClose() { return close; }
	//double GetHigh() { return high; }
	//double GetLow() { return low; }
	//double GetBase() { return base; }
	//double GetCurrent() { return current; }
	//double GetVolume() { return volume; }
	//double GetMarketCap() { return marketCap; }
	//double GetCapital() { return capital; }
	//double GetFaceValue() { return faceValue; }
	//double GetSales() { return sales; }
	//double GetOperatingIncome() { return operatingIncome; }
	//double GetNetIncome() { return netIncome; }
	//double GetOutstandingShare() { return outstandingShare; }
	//double GetROE() { return roe; }
	//double GetPER() { return per; }
	//double GetPBR() { return pbr; }
	//double GetEV() { return ev; }
	//int GetCode() { return code; }
	//int GetNumOfStock () { return numOfStock; }
	//CString GetStockName() { return stockName; }
	//CString GetCreditRatio() { return creditRatio; }

};
